<div x-data="{ open: false }">
    <button class="btn btn-blue" @click="open = true">Open Dropdown</button>

    <ul
        x-show="open"
        @click.away="open = false"
    >
        Dropdown Body
    </ul>
</div>
